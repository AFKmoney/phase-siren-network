[project]
name = "phase-siren-network"
version = "0.2.0"
description = "Non-gradient neural computation via hash-constructed specialized experts"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
dependencies = ["numpy>=1.24"]

[project.optional-dependencies]
dev = []

[tool.setuptools.packages.find]
include = ["psn*"]
